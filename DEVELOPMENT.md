# 🔧 Разработка - NEAR Analytics Bot

## 🎯 Ситуация

- ✅ Telegram бот работает на **Railway** (production)
- ✅ Frontend работает на **Vercel** (production)
- ✅ Код в **GitHub** репозитории

## ⚠️ Проблема локального запуска

**Нельзя запустить бота локально** если он уже работает на Railway!

Telegram Bot API не позволяет двум экземплярам с одним токеном работать одновременно.

Ошибка:
```
409: Conflict: terminated by other getUpdates request
```

---

## ✅ Решения

### Вариант 1: Локальная разработка (РЕКОМЕНДУЕТСЯ)

Запускаем **только API и Frontend** локально для тестирования:

```batch
dev-local.bat
```

**Что запустится:**
- ✅ Backend API на `http://localhost:3001` (БЕЗ Telegram бота)
- ✅ Frontend на `http://localhost:5173`
- ✅ Telegram бот продолжает работать на Railway

**Когда использовать:**
- Разработка новых функций
- Тестирование API
- Изменения Frontend
- Тестирование аналитики

---

### Вариант 2: Деплой без локального запуска

Вносим изменения → коммитим → деплоим → тестируем на production:

```batch
# 1. Вносим изменения в код
# Редактируем файлы...

# 2. Быстрый деплой
quick-deploy.bat
> fix: улучшена аналитика

# 3. Ждём автодеплоя на Railway (если настроен webhook)
# или деплоим вручную через Railway dashboard
```

**Когда использовать:**
- Быстрые фиксы
- Мелкие правки
- Когда не нужно тестировать локально

---

### Вариант 3: Временная остановка Production бота

**⚠️ НЕ РЕКОМЕНДУЕТСЯ** (бот перестанет работать для пользователей!)

1. Остановите бота на Railway:
   - Railway Dashboard → Project → Stop

2. Запустите локально:
   ```batch
   npm run dev
   ```

3. После разработки - верните бот на Railway

**Когда использовать:**
- Только для экстренного дебага
- Когда нужно проверить работу самого бота

---

## 🚀 Рабочий процесс разработки

### Типичный день разработки:

```batch
# Утро - начинаем работу
dev-local.bat

# Редактируем код
# Файлы обновляются автоматически (hot reload)

# Тестируем в браузере
# http://localhost:5173

# Тестируем API
curl http://localhost:3001/api/analytics/leninjiv23.tg?period=week

# Коммитим изменения
quick-deploy.bat
> feat: добавлена новая аналитика

# Railway автоматически задеплоит
# Проверяем на production
```

---

## 📋 Структура разработки

### Backend (Node.js + Express)

```
src/
├── api.js              ← REST API (можно запускать локально)
├── index.js            ← Telegram бот (НЕ запускать если работает на Railway)
├── services/
│   └── nearService.js  ← Бизнес-логика (тестируем через API)
└── commands/           ← Команды бота
```

### Frontend (React + Vite)

```
webapp/
├── src/
│   ├── components/     ← React компоненты
│   ├── services/       ← API клиент
│   └── hooks/          ← React хуки
└── public/
```

---

## 🧪 Тестирование

### Тестирование API локально

```bash
# Запустить API
dev-local.bat

# В другом терминале
curl http://localhost:3001/api/health
curl http://localhost:3001/api/balance/leninjiv23.tg
curl http://localhost:3001/api/analytics/leninjiv23.tg?period=week
curl http://localhost:3001/api/nfts/leninjiv23.tg
```

### Тестирование Frontend локально

1. Запустите `dev-local.bat`
2. Откройте `http://localhost:5173`
3. Проверьте:
   - Переключение периодов (7/30/90 дней)
   - Загрузку NFT
   - Отображение аналитики

### Тестирование на Production

1. Откройте ваш Vercel URL
2. Проверьте через Telegram бота
3. Проверьте логи на Railway

---

## 🔄 Git Workflow

### Быстрые изменения

```batch
# Редактируем код...

quick-deploy.bat
> fix: исправлен баг

# Railway автоматически деплоит
```

### Крупные фичи

```batch
# Редактируем код...

# Проверяем статус
git-status.bat

# Полный деплой с описанием
deploy-functionality-update.bat

# Railway деплоит автоматически
```

### Откат изменений

```batch
# Если что-то сломалось
git-undo.bat
> Выбрать: 2 (отменить последний коммит, оставить изменения)

# Исправить и закоммитить заново
quick-deploy.bat
```

---

## 🌐 Переменные окружения

### Локальная разработка (.env)

```env
# НЕ ИСПОЛЬЗУЕТСЯ при локальной разработке (бот на Railway)
BOT_TOKEN=your_token_here

# API работает локально
API_PORT=3001

# Frontend локально
WEBAPP_URL=http://localhost:5173

# API keys (опционально)
NEARBLOCKS_API_KEY=your_key
PIKESPEAK_API_KEY=your_key
```

### Production (Railway)

```env
BOT_TOKEN=your_production_token
WEBAPP_URL=https://your-app.vercel.app
API_PORT=3001
```

### Frontend (Vercel)

```env
# .env в webapp/
VITE_API_URL=http://localhost:3001          # Локально
VITE_API_URL=https://your-app.railway.app   # Production
```

---

## 🐛 Типичные проблемы

### 1. "409 Conflict" при запуске

**Проблема:** Бот уже работает на Railway

**Решение:** Используйте `dev-local.bat` (без бота)

---

### 2. Frontend не подключается к API

**Проблема:** Неправильный `VITE_API_URL`

**Решение:**
```bash
# В webapp/.env
VITE_API_URL=http://localhost:3001
```

Перезапустите Frontend:
```bash
cd webapp
npm run dev
```

---

### 3. Изменения не применяются

**Проблема:** Кэш браузера или hot reload не сработал

**Решение:**
1. Ctrl+F5 (принудительная перезагрузка)
2. Или перезапустите `dev-local.bat`

---

### 4. API возвращает ошибки

**Проблема:** NEAR API недоступен или превышен rate limit

**Решение:**
1. Проверьте логи в консоли Backend
2. Проверьте интернет соединение
3. Подождите 1-2 минуты (rate limit)

---

## 📊 Мониторинг Production

### Railway

1. Dashboard → Logs
2. Смотрите логи в реальном времени
3. Проверяйте метрики (CPU, Memory)

### Vercel

1. Dashboard → Deployments
2. Проверяйте статус деплоя
3. Смотрите логи функций

### Telegram Bot

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Проверьте команды

---

## 💡 Лучшие практики

### Код

- ✅ Используйте `dev-local.bat` для разработки
- ✅ Коммитьте часто (после каждой фичи)
- ✅ Пишите понятные commit messages
- ✅ Тестируйте локально перед деплоем

### Деплой

- ✅ Используйте `quick-deploy.bat` для быстрых фиксов
- ✅ Используйте `deploy-functionality-update.bat` для крупных фич
- ✅ Проверяйте production после каждого деплоя
- ✅ Держите backup `.env` файла

### Безопасность

- ✅ НЕ коммитьте `.env` файлы
- ✅ Используйте `.env.example` для примеров
- ✅ Проверяйте `.gitignore` перед коммитом
- ✅ Используйте разные токены для dev и prod (если возможно)

---

## 📚 Документация

- [README.md](README.md) - Основная документация
- [TESTING.md](TESTING.md) - Руководство по тестированию
- [CHANGELOG.md](CHANGELOG.md) - История изменений
- [GIT_COMMANDS.md](GIT_COMMANDS.md) - Git команды
- [BAT_FILES.md](BAT_FILES.md) - BAT-файлы (если создан)

---

## 🎯 Краткая памятка

```batch
# Начало работы
dev-local.bat

# Быстрый коммит
quick-deploy.bat

# Полный деплой
deploy-functionality-update.bat

# Проверить статус
git-status.bat

# Остановить всё
stop-bot.bat  # останавливает локальные процессы
```

---

**Удачной разработки! 🚀**
