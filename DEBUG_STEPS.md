# 🔍 Пошаговая отладка Vercel

## Что изменили:

1. ✅ Добавлен скрипт `vercel-build` в `package.json`
2. ✅ Упрощен `vercel.json` 
3. ✅ Добавлен `.vercelignore`
4. ✅ Настроены functions в `vercel.json`

---

## ⚡ Закоммитьте изменения:

```bash
cd "c:\Users\la1wo\OneDrive\Рабочий стол\web3\near-analytics-bot"
git add .
git commit -m "fix: Simplify Vercel config and add vercel-build script"
git push origin master
```

---

## 🧪 Тесты после деплоя:

### Тест 1: Проверьте что API работает
```
https://near-pulse.vercel.app/api/index
```

Должно вернуть JSON с информацией об API.

### Тест 2: Проверьте health
```
https://near-pulse.vercel.app/api/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "timestamp": ...
}
```

### Тест 3: Проверьте корень
```
https://near-pulse.vercel.app/
```

Должна загрузиться React приложение (не 401).

### Тест 4: Проверьте статику
```
https://near-pulse.vercel.app/vite.svg
```

Должен вернуть SVG файл (не 401).

---

## 📊 Если всё ещё 401:

### Шаг 1: Проверьте Build Logs

В Vercel Dashboard → Deployments → последний деплой → Build Logs

**Ищите:**
- ✅ "Building webapp..." - значит фронтенд собирается
- ✅ "Build completed" - сборка успешна
- ❌ Любые ошибки при сборке

### Шаг 2: Проверьте Files Output

В Vercel Dashboard → Deployments → последний деплой → Output Files

**Должны быть:**
- `webapp/dist/index.html` ✅
- `webapp/dist/assets/...` ✅
- `api/index.js` ✅

Если `webapp/dist/` пустая - значит сборка не работает!

### Шаг 3: Проверьте Functions

В Vercel Dashboard → Functions

**Должна быть функция:**
- `api/index` ✅

Кликните на неё → Runtime Logs → посмотрите логи

---

## 🚨 Альтернативное решение

Если ничего не помогает, попробуйте **разделить проекты**:

### Вариант 1: Два отдельных Vercel проекта
- Frontend на `near-pulse.vercel.app`
- API на `near-pulse-api.vercel.app`

### Вариант 2: API на другом сервисе
- Frontend на Vercel
- API на Railway / Render

Но сначала попробуйте текущий подход! 💪

---

## 📝 Что показать для помощи:

1. **Screenshot Build Logs** - вся сборка целиком
2. **Screenshot Output Files** - список файлов после сборки
3. **Screenshot Functions Logs** - логи функции api/index
4. **Screenshot Browser Console** - ошибки в F12

С этими данными я точно найду проблему! 🎯
