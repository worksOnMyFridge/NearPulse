# Тестирование NEAR Analytics Bot

## Запуск проекта

### Backend API

```bash
cd near-analytics-bot
npm install
npm run dev
```

API будет доступен на `http://localhost:3001`

### Frontend Webapp

```bash
cd near-analytics-bot/webapp
npm install
npm run dev
```

Webapp будет доступен на `http://localhost:5173`

## Тестирование функций

### 1. Тестирование динамических периодов

**Endpoint:** `GET /api/analytics/:address?period=week|month|all`

**Примеры запросов:**

```bash
# 7 дней
curl "http://localhost:3001/api/analytics/leninjiv23.tg?period=week"

# 30 дней
curl "http://localhost:3001/api/analytics/leninjiv23.tg?period=month"

# Всё время (90 дней)
curl "http://localhost:3001/api/analytics/leninjiv23.tg?period=all"
```

**Проверка на фронтенде:**

1. Откройте webapp в браузере
2. Кликните на кнопки "7 дней", "30 дней", "Всё время"
3. Проверьте что данные обновляются:
   - График "Активность по дням" меняет масштаб
   - Статистика по газу пересчитывается
   - Топ протоколы обновляются

### 2. Тестирование NFT

**Endpoint:** `GET /api/nfts/:address`

**Пример запроса:**

```bash
curl "http://localhost:3001/api/nfts/leninjiv23.tg"
```

**Структура ответа:**

```json
{
  "address": "leninjiv23.tg",
  "wallet": [
    {
      "contract": "nft.contract.near",
      "token_id": "1234",
      "title": "NFT Title",
      "description": "NFT Description",
      "media": "https://...",
      "collection": "nft.contract.near"
    }
  ],
  "hotStaked": [],
  "total": 1,
  "timestamp": 1234567890
}
```

**Проверка на фронтенде:**

1. Откройте webapp
2. Прокрутите вниз до секции "Мои NFT"
3. Проверьте:
   - NFT из кошелька отображаются с изображениями
   - Застейканные NFT в HOT отмечены специальной иконкой 🔥
   - Счётчик показывает правильное количество

### 3. Тестирование HOT Craft API (застейканные NFT)

**Важно:** Для тестирования нужен адрес с застейканными NFT в HOT Craft.

**Метод контракта:** `game.hot.tg::get_user`

**Тестирование вручную через NEAR RPC:**

1. Откройте консоль разработчика на странице webapp
2. Проверьте логи при загрузке NFT:
   ```
   [HOT Staked NFTs] User data получена: {...}
   🔥 [HOT] Найдено X застейканных NFT
   ```

**Известные ограничения:**

- Метод `get_user` контракта `game.hot.tg` возвращает данные пользователя
- Структура поля с NFT может отличаться (нужно проверить с реальным аккаунтом)
- Возможные варианты полей: `staked_nfts`, `nfts`, `tokens`, `staked_tokens`

**Если NFT не отображаются:**

1. Проверьте логи в консоли браузера
2. Проверьте логи сервера (backend)
3. Убедитесь что у тестового адреса есть застейканные NFT в HOT
4. Проверьте структуру ответа `get_user` в логах

### 4. Проверка Gas аналитики с процентами

**Проверка на фронтенде:**

1. Перейдите на вкладку "Аналитика"
2. В секции "Gas по протоколам" проверьте:
   - Каждый протокол показывает иконку + название
   - Под названием указан процент от общего gas
   - Справа отображается точное количество NEAR и транзакций

**Пример отображения:**

```
🔥 Hot Protocol
   45% от общего gas
                    0.0543 N
                    20 txs
```

## Отладка

### Backend логи

Включите режим разработки в `.env`:

```env
NODE_ENV=development
```

Логи будут выводиться в консоль:

```
[getAnalytics] Analyzing 47 transactions for period week
💵 [CoinGecko] Получены цены для 5 токенов
🎨 [NFT] Найдено 3 NFT для leninjiv23.tg
```

### Frontend логи

Откройте DevTools → Console для просмотра логов:

```
[App] Загружаем баланс для: leninjiv23.tg
[AnalyticsScreen] Аналитика получена: {...}
[OverviewScreen] NFT загружены: {...}
```

## Типичные проблемы

### 1. CORS ошибки

**Симптом:** Ошибка в консоли "Access-Control-Allow-Origin"

**Решение:** Убедитесь что в `.env` указан правильный `WEBAPP_URL`:

```env
WEBAPP_URL=http://localhost:5173
```

### 2. API недоступен

**Симптом:** "Failed to fetch" в консоли браузера

**Решение:** 

1. Проверьте что backend запущен: `curl http://localhost:3001/api/health`
2. Проверьте `VITE_API_URL` в `webapp/.env`:
   ```env
   VITE_API_URL=http://localhost:3001
   ```

### 3. Нет данных аналитики

**Симптом:** "Нет транзакций за этот период"

**Причины:**

1. У адреса действительно нет транзакций
2. Nearblocks API недоступен
3. Период слишком короткий

**Решение:**

- Попробуйте другой период
- Проверьте адрес в NEAR Explorer
- Проверьте логи backend на наличие ошибок API

## Деплой на Railway

### Подготовка

1. Убедитесь что `.env` не попадает в git:
   ```bash
   git status
   # .env должен быть в .gitignore
   ```

2. Добавьте переменные окружения в Railway:
   - `NEARBLOCKS_API_KEY` (если есть)
   - `PIKESPEAK_API_KEY` (если есть)
   - `WEBAPP_URL` (URL вашего фронтенда на Vercel/Netlify)
   - `PORT=3001`

3. Railway автоматически обнаружит Node.js проект и запустит `npm start`

### Проверка после деплоя

```bash
# Health check
curl https://your-app.railway.app/api/health

# Test analytics
curl https://your-app.railway.app/api/analytics/leninjiv23.tg?period=week

# Test NFTs
curl https://your-app.railway.app/api/nfts/leninjiv23.tg
```

## HOT Craft API - Дополнительные исследования

Для полной интеграции с HOT Craft нужно:

1. **Найти аккаунт с застейканными NFT:**
   - Откройте https://t.me/herewalletbot
   - Найдите пользователя с активным стейкингом NFT
   - Получите его NEAR адрес

2. **Протестировать `get_user`:**
   ```javascript
   const { getHotStakedNFTs } = require('./src/services/nearService');
   
   getHotStakedNFTs('address.near').then(console.log);
   ```

3. **Изучить структуру ответа:**
   - Проверьте какие поля содержат информацию о NFT
   - Обновите функцию `getHotStakedNFTs` если нужно

4. **Возможные варианты структуры:**
   ```javascript
   // Вариант 1
   userData.staked_nfts = [{ contract, token_id, ... }]
   
   // Вариант 2
   userData.nfts = [{ ... }]
   
   // Вариант 3
   userData.tokens = { nfts: [...] }
   ```

5. **Обновите код если нужно:**
   ```javascript
   // В nearService.js, функция getHotStakedNFTs
   const stakedNFTs = userData.staked_nfts || 
                      userData.nfts || 
                      userData.tokens?.nfts || 
                      [];
   ```
