# 🚀 Гайд по деплою - NEAR Analytics Bot

## 📌 Текущая ситуация

- ✅ **Telegram бот** работает на **Railway**
- ✅ **Frontend** работает на **Vercel**
- ✅ **Код** в **GitHub**

---

## ⚡ Быстрый деплой изменений

### 1️⃣ Вносим изменения в код

Редактируем нужные файлы в VSCode или другом редакторе

### 2️⃣ Коммитим и пушим в GitHub

```batch
quick-deploy.bat
> Введите описание: feat: улучшена аналитика NFT
```

Или детальный деплой:

```batch
deploy-functionality-update.bat
```

### 3️⃣ Railway автоматически задеплоит

Если настроен GitHub webhook:
- Railway автоматически подхватит изменения
- Backend обновится через 1-2 минуты
- Проверьте логи на Railway Dashboard

Если webhook НЕ настроен:
- Railway Dashboard → Deploy → Manual Deploy

### 4️⃣ Vercel автоматически задеплоит Frontend

Если настроен GitHub webhook:
- Vercel подхватит изменения в `webapp/`
- Frontend обновится через 1-2 минуты

Если webhook НЕ настроен:
```bash
cd webapp
vercel --prod
```

---

## 🧪 Тестирование перед деплоем (опционально)

### Локальное тестирование API и Frontend

```batch
# Запускаем локально (БЕЗ бота)
dev-local.bat

# Открываем в браузере
http://localhost:5173

# Тестируем функции
# После - коммитим
```

---

## 📋 Workflow для разных ситуаций

### Сценарий 1: Быстрый фикс (5 минут)

```batch
# Редактируем код
# src/services/nearService.js

# Коммитим
quick-deploy.bat
> fix: исправлен баг с балансом

# Railway деплоит автоматически
# Готово! ✅
```

---

### Сценарий 2: Новая фича (30+ минут)

```batch
# Редактируем код
# src/services/nearService.js
# src/api.js
# webapp/src/components/...

# Тестируем локально (опционально)
dev-local.bat
# Открываем http://localhost:5173
# Проверяем

# Коммитим с подробным описанием
deploy-functionality-update.bat

# Railway + Vercel деплоят автоматически
# Проверяем на production
# Готово! ✅
```

---

### Сценарий 3: Только Frontend изменения

```batch
# Редактируем код
# webapp/src/components/AnalyticsScreen.jsx

# Коммитим
quick-deploy.bat
> feat: улучшен UI аналитики

# Vercel деплоит автоматически
# Готово! ✅
```

---

### Сценарий 4: Только Backend изменения

```batch
# Редактируем код
# src/services/nearService.js
# src/api.js

# Коммитим
quick-deploy.bat
> feat: добавлен новый endpoint

# Railway деплоит автоматически
# Готово! ✅
```

---

## 🔧 Важные команды

### Git

```batch
git-status.bat              # Проверить статус
quick-deploy.bat            # Быстрый деплой
deploy-functionality-update.bat  # Детальный деплой
git-undo.bat                # Отменить изменения
```

### Разработка

```batch
dev-local.bat               # Локальное тестирование
stop-bot.bat                # Остановить локальные процессы
```

### Установка/обновление

```batch
install.bat                 # Установить зависимости
update.bat                  # Обновить пакеты
clean.bat                   # Очистить node_modules
```

---

## 📊 Проверка после деплоя

### 1. Backend (Railway)

```bash
# Проверка здоровья API
curl https://your-app.railway.app/api/health

# Проверка endpoint
curl https://your-app.railway.app/api/analytics/leninjiv23.tg?period=week
```

### 2. Frontend (Vercel)

- Откройте ваш Vercel URL
- Проверьте все экраны
- Протестируйте функционал

### 3. Telegram Bot

- Откройте бота в Telegram
- Отправьте команды
- Проверьте что всё работает

---

## ⚠️ Проблемы и решения

### "409 Conflict" при локальном запуске

**Проблема:** Бот уже работает на Railway

**Решение:** Используйте `dev-local.bat` вместо `npm run dev`

---

### Изменения не применяются

**Проблема:** Деплой не запустился

**Решение:**
1. Проверьте Railway/Vercel Dashboard
2. Проверьте что изменения попали в GitHub
3. Запустите ручной деплой если нужно

---

### Railway показывает ошибки

**Проблема:** Ошибка при деплое на Railway

**Решение:**
1. Railway Dashboard → Logs
2. Найдите ошибку
3. Исправьте код
4. Закоммитьте и задеплойте снова

---

## 💡 Советы

1. **Коммитьте часто** - после каждой фичи
2. **Пишите понятные сообщения** - `fix: исправлен баг`, `feat: новая фича`
3. **Тестируйте локально** перед деплоем (опционально)
4. **Проверяйте production** после каждого деплоя
5. **Держите backup** `.env` файла

---

## 🎯 Краткая шпаргалка

```
┌─────────────────────────────────────────┐
│ Код → quick-deploy.bat → GitHub → Done │
└─────────────────────────────────────────┘

Railway автоматически деплоит Backend ⚡
Vercel автоматически деплоит Frontend ⚡
```

---

**Успешного деплоя! 🚀**
