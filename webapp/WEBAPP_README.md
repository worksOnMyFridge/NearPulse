# NearPulse Webapp - React приложение

Telegram Mini App для просмотра аналитики NEAR кошелька.

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
cd webapp
npm install
```

### 2. Настройте переменные окружения

В файле `.env` уже настроено:
```env
VITE_API_URL=http://localhost:3001
```

### 3. Запустите приложение

**Важно:** Сначала запустите API сервер (см. ../API_README.md)

```bash
# В корне проекта (near-analytics-bot/)
npm run api
```

Затем запустите webapp:

```bash
# В папке webapp/
npm run dev
```

Приложение откроется на `http://localhost:5173`

## 📁 Структура проекта

```
webapp/
├── src/
│   ├── components/
│   │   ├── Header.jsx           # Шапка с табами
│   │   ├── OverviewScreen.jsx   # Главный экран с балансом
│   │   ├── TransactionsScreen.jsx
│   │   ├── AnalyticsScreen.jsx
│   │   └── LoadingSpinner.jsx
│   ├── services/
│   │   └── api.js               # API клиент (НОВЫЙ)
│   ├── hooks/
│   │   └── useTelegram.js       # Telegram WebApp SDK
│   ├── lib/
│   │   └── mockData.js          # Моковые данные для фоллбека
│   ├── App.jsx                  # Главный компонент (ОБНОВЛЁН)
│   └── main.jsx
├── .env                         # Переменные окружения (НОВЫЙ)
└── package.json
```

## ✨ Что реализовано

### API интеграция
- ✅ `src/services/api.js` - клиент для работы с REST API
- ✅ `fetchUserBalance(address)` - получение баланса аккаунта

### Обновлённые компоненты
- ✅ **App.jsx** - загрузка данных с API при монтировании
- ✅ **OverviewScreen.jsx** - отображение реальных данных:
  - Карточка с балансом NEAR и HOT
  - Insights с реальными цифрами
  - Замена "45,000 MOON" на реальный HOT баланс

### Обработка состояний
- ✅ Показ загрузки (LoadingSpinner)
- ✅ Обработка ошибок с подсказками
- ✅ Автоматическая загрузка при смене адреса

## 🔧 Как это работает

1. **App.jsx** вызывает `fetchUserBalance()` при загрузке
2. API возвращает данные: NEAR, HOT, токены
3. **OverviewScreen** получает `balanceData` через props
4. Реальные данные заменяют моковые:
   - Карточка портфеля показывает NEAR и HOT
   - Insights обновляются с реальными цифрами

## 📊 Пример данных из API

```json
{
  "address": "leninjiv23.tg",
  "near": {
    "available": 125.5,
    "staked": 50.0,
    "total": 175.5,
    "price": 1.07,
    "usdValue": 187.79
  },
  "hot": {
    "amount": 12345.67
  },
  "tokens": {
    "major": [...],
    "filtered": [...],
    "hidden": [...]
  }
}
```

## 🐛 Отладка

### Проверка подключения к API

Откройте браузер и проверьте:
```
http://localhost:3001/api/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "timestamp": 1234567890,
  "service": "NearPulse API"
}
```

### Логи в консоли

В консоли браузера должно появиться:
```
[App] Загружаем баланс для: leninjiv23.tg
[App] Баланс получен: {...}
```

## 🚢 Деплой на Vercel

Webapp уже задеплоен на: https://near-pulse.vercel.app/

Для обновления:
1. Закоммитьте изменения в git
2. Пушьте в репозиторий
3. Vercel автоматически задеплоит

**Важно:** Для production обновите `.env`:
```env
VITE_API_URL=https://your-production-api.com
```

## 📝 Следующие шаги

- [ ] Добавить endpoint для транзакций
- [ ] Отобразить список токенов из API
- [ ] Добавить кэширование данных
- [ ] Добавить pull-to-refresh
