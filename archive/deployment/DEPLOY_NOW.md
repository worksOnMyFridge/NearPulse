# 🚀 ГОТОВО К ДЕПЛОЮ - Финальная инструкция

## ✅ Что сделано:

1. ✅ **vercel.json** - конфигурация создана и валидна
2. ✅ **api/index.js** - serverless function настроена
3. ✅ **src/api.js** - Express app экспортируется правильно
4. ✅ **package.json** - все зависимости на месте (express, cors)
5. ✅ **webapp** - использует умный API URL (production/development)

---

## 📋 ДВА действия для деплоя:

### Действие 1: Коммит и пуш

Откройте терминал и выполните:

```bash
git add .
git commit -m "feat: Configure Express API for Vercel deployment with proper routing"
git push origin master
```

### Действие 2: Принудительный редеплой на Vercel

**Вариант А: Через Dashboard (рекомендуется)**
1. Зайдите на https://vercel.com
2. Откройте проект **NearPulse**
3. Вкладка **Deployments**
4. Найдите последний деплой → три точки ⋯ → **Redeploy**
5. ❗ ВАЖНО: Снимите галку **"Use existing Build Cache"**
6. Нажмите **Redeploy**

**Вариант Б: Пустой коммит (быстрее)**
```bash
git commit --allow-empty -m "chore: trigger Vercel redeploy"
git push origin master
```

Vercel автоматически начнёт новый деплой!

---

## 🔍 Что изменилось в API:

### До:
```javascript
app.get('/api/health', ...);  // Только один роут
```

### После:
```javascript
app.get(['/api/health', '/health'], ...);  // Двойные роуты
```

**Почему:** Когда Vercel перенаправляет `/api/health` → `/api/index`, Express видит путь как `/health` (без `/api` префикса).

Теперь оба варианта работают:
- ✅ Локально: `http://localhost:3001/api/health`
- ✅ Vercel: `https://near-pulse.vercel.app/api/health` → роут `/health`

---

## 🎯 Проверка после деплоя:

**1. Проверьте API напрямую:**
```
https://near-pulse.vercel.app/api/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "timestamp": 1234567890,
  "service": "NearPulse API",
  "environment": "vercel"
}
```

**2. Проверьте баланс:**
```
https://near-pulse.vercel.app/api/balance/leninjiv23.tg
```

Должно вернуть JSON с балансом.

**3. Откройте webapp:**
```
https://near-pulse.vercel.app/
```

Должен показаться реальный баланс NEAR и HOT!

---

## ❌ Если всё ещё ошибка:

### Откройте Vercel Dashboard → Functions → Logs

Покажите мне что там написано. Я увижу точную ошибку и исправлю!

Типичные ошибки:
- `Cannot find module` - не хватает зависимости
- `Timeout` - NEAR API не отвечает (нужно увеличить timeout)
- `500 Internal Error` - ошибка в коде (смотрим stack trace)

---

## 🎉 ГОТОВО!

Файлы подготовлены правильно. Осталось только:

1. **Закоммитить** изменения
2. **Пушнуть** в GitHub
3. **Редеплоить** на Vercel (принудительно, без кэша)

И всё заработает! 🚀✨
