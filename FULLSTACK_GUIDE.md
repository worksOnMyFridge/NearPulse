# 🚀 NearPulse - Полное руководство по запуску

Система состоит из 3 частей:
1. **Telegram Bot** - обрабатывает команды в Telegram
2. **REST API** - предоставляет данные для webapp
3. **React Webapp** - Telegram Mini App интерфейс

---

## 📦 Установка зависимостей

```bash
# Корневая директория (Bot + API)
npm install

# Webapp
cd webapp
npm install
cd ..
```

---

## 🎯 Запуск всей системы

### Вариант 1: Запуск всего (рекомендуется)

Откройте **3 терминала**:

**Терминал 1 - Telegram Bot:**
```bash
npm start
```
✅ Бот запущен и обрабатывает команды в Telegram

**Терминал 2 - REST API:**
```bash
npm run api
```
✅ API доступен на http://localhost:3001

**Терминал 3 - React Webapp:**
```bash
cd webapp
npm run dev
```
✅ Webapp открыт на http://localhost:5173

---

### Вариант 2: Только Telegram Bot

Если webapp не нужен:
```bash
npm start
```

---

## ✅ Проверка работы

### 1. Проверка API
Откройте в браузере:
```
http://localhost:3001/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "timestamp": 1234567890,
  "service": "NearPulse API"
}
```

### 2. Проверка данных
```
http://localhost:3001/api/balance/leninjiv23.tg
```

Должен вернуть баланс:
```json
{
  "address": "leninjiv23.tg",
  "near": {
    "total": 175.5,
    "price": 1.07
  },
  "hot": {
    "amount": 12345.67
  }
}
```

### 3. Проверка Webapp
Откройте:
```
http://localhost:5173
```

Должны увидеть:
- ⏳ Индикатор загрузки
- 💰 Карточку с балансом NEAR и HOT
- 📊 Insights с реальными данными

---

## 🔧 Структура проекта

```
near-analytics-bot/
├── src/
│   ├── index.js           # Telegram Bot (порт: Telegram)
│   ├── api.js             # REST API (порт: 3001) ⭐ НОВЫЙ
│   ├── services/
│   │   └── nearService.js # Логика работы с NEAR API
│   └── config/
│       └── constants.js
├── webapp/                # React приложение (порт: 5173) ⭐ ОБНОВЛЁН
│   ├── src/
│   │   ├── App.jsx        # Загрузка данных с API ⭐
│   │   ├── services/
│   │   │   └── api.js     # API клиент ⭐ НОВЫЙ
│   │   └── components/
│   │       └── OverviewScreen.jsx  # Реальные данные ⭐
│   └── .env               # VITE_API_URL ⭐ НОВЫЙ
├── .env                   # API_PORT=3001 ⭐ ОБНОВЛЁН
└── package.json
```

---

## 📱 Что изменилось

### ✅ Созданы новые файлы:
1. `src/api.js` - Express REST API сервер
2. `webapp/src/services/api.js` - API клиент
3. `webapp/.env` - конфигурация для Vite
4. Документация: API_README.md, WEBAPP_README.md

### ✅ Обновлены файлы:
1. `package.json` - добавлены express, cors, скрипты api
2. `.env` - добавлен API_PORT=3001
3. `webapp/src/App.jsx` - загрузка данных с API
4. `webapp/src/components/OverviewScreen.jsx` - реальные данные

### ✅ Интеграция работает:
- React загружает данные с API
- "45,000 MOON" заменено на реальный HOT баланс
- Показывается баланс NEAR с USD эквивалентом
- Обработка загрузки и ошибок

---

## 🎨 Что вы увидите в Webapp

### Было (моковые данные):
```
📊 Insights:
- Вы на 30% активнее чем обычно
- Gas расходы выше среднего на 15%
- Получено 45,000 MOON токенов ⬅️ MOCK
```

### Стало (реальные данные):
```
💰 Портфель:
NEAR: 175.50 ($187.79)
HOT: 12,345 токенов

📊 Insights:
- NEAR баланс: 175.50 NEAR (~$187.79) ⬅️ REAL
- HOT баланс: 12,345 токенов ⬅️ REAL
- Gas расходы выше среднего на 15%
```

---

## 🐛 Troubleshooting

### Проблема: "Failed to fetch" в webapp
**Решение:** Убедитесь что API запущен:
```bash
npm run api
```

### Проблема: CORS ошибка
**Решение:** API настроен на localhost:5173, проверьте что webapp на этом порту

### Проблема: "Cannot find module 'express'"
**Решение:**
```bash
npm install
```

---

## 📚 Полезные команды

```bash
# Telegram Bot
npm start              # Запуск бота
npm run dev            # Разработка с авто-перезагрузкой

# REST API
npm run api            # Запуск API
npm run api:dev        # Разработка с авто-перезагрузкой

# React Webapp
cd webapp
npm run dev            # Запуск dev сервера
npm run build          # Сборка для продакшена
```

---

## 🚢 Деплой в продакшен

1. **Webapp** - уже на Vercel: https://near-pulse.vercel.app/
2. **API** - задеплойте на VPS/Heroku/Railway
3. Обновите `webapp/.env` с production URL:
   ```env
   VITE_API_URL=https://your-api.com
   ```

---

## ✨ Готово!

Теперь у вас полноценный fullstack NEAR Analytics дашборд! 🎉
