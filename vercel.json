{
  "installCommand": "cd webapp && npm install",
  "buildCommand": "cd webapp && npx vite build",
  "outputDirectory": "webapp/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
