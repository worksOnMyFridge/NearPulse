# 🚂 Railway Setup - 2 Сервиса

## Проблема
Railway запускает только `src/api.js` (Web API), а бот (`src/index.js`) не работает!

## Решение: 2 отдельных сервиса

---

## 📋 Вариант 1: Один проект, 2 сервиса (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Текущий сервис → API

1. Railway Dashboard → ваш проект
2. Settings → Service Name → переименуйте в **"API"**
3. Settings → Start Command → `node src/api.js`
4. ✅ Это будет Web API сервер

### Шаг 2: Создать новый сервис для бота

1. В том же проекте нажмите **"+ New Service"**
2. Выберите ваш GitHub репозиторий
3. Service Name: **"Bot"**
4. Settings → Start Command → `node src/index.js`
5. Variables → скопируйте все переменные из API сервиса
6. ✅ Это будет Telegram Bot

### Результат:
```
Проект: NearPulse
├── Сервис 1: API (src/api.js) → https://your-api.railway.app
└── Сервис 2: Bot (src/index.js) → работает в фоне
```

---

## 📋 Вариант 2: Один сервис с Procfile

Если хотите оба в одном сервисе (НЕ рекомендуется, но возможно):

### 1. Создайте `Procfile`:
```
web: node src/api.js
worker: node src/index.js
```

### 2. В Railway Settings:
- Start Command: оставьте пустым (Railway прочитает Procfile)
- ✅ Railway запустит оба процесса

---

## ⚙️ Переменные окружения

Оба сервиса должны иметь:

```
TELEGRAM_BOT_TOKEN=...
NEARBLOCKS_API_KEY=(может быть пустым)
GOOGLE_API_KEY=...
PIKESPEAK_API_KEY=...
WEBAPP_URL=https://near-pulse.vercel.app/
API_PORT=3001
```

---

## 🧪 Проверка

### Проверить API:
```bash
curl https://your-api.railway.app/api/health
```

**Должно вернуть:**
```json
{
  "status": "ok"
}
```

### Проверить бота:
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должен ответить!

---

## 🔍 Логи

### Логи API (Web):
Railway → API Service → Deploy Logs

### Логи Bot (Telegram):
Railway → Bot Service → Deploy Logs

**Ищите:**
```
Bot started successfully ✅
Listening on port 3001 (для API)
```

---

## 🚨 Troubleshooting

### Бот не отвечает:
1. Проверьте Bot Service запущен (не crashed)
2. Проверьте Deploy Logs на ошибки
3. Убедитесь что `TELEGRAM_BOT_TOKEN` правильный

### API возвращает 500:
1. Проверьте API Service Deploy Logs
2. Убедитесь что все переменные окружения добавлены
3. Проверьте что нет ошибок в коде

---

## ✅ Быстрая настройка (Вариант 1)

1. **Переименовать текущий сервис:**
   - Railway → Settings → Name: "API"
   - Start Command: `node src/api.js`

2. **Создать Bot сервис:**
   - "+ New Service" → GitHub → ваш репо
   - Name: "Bot"
   - Start Command: `node src/index.js`
   - Variables: скопировать все из API

3. **Deploy оба:**
   - Git push автоматически деплоит оба сервиса!

---

## 🎯 Итого

После настройки:
- ✅ API работает: `https://your-api.railway.app/api/health`
- ✅ Бот работает: `/start` в Telegram отвечает
- ✅ Webapp работает: `https://near-pulse.vercel.app`

Оба сервиса деплоятся автоматически при `git push`! 🚀
