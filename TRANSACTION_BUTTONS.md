# 🎯 Transaction Action Buttons - Feature Documentation

## Обзор

Добавлены функциональные кнопки действий к каждой транзакции в списке, реализованные в минималистичном Apple-стиле с поддержкой Dark/Light тем.

---

## 🔘 Кнопки Действий

### 1. 🌐 Explorer
**Функция:** Открывает транзакцию в Nearblocks Explorer

**Поведение:**
- Ссылка на `https://nearblocks.io/txns/{hash}`
- Открывается в новой вкладке
- Hover эффект: синий акцент

**Иконка:** `Globe` (lucide-react)

---

### 2. 📋 Копировать
**Функция:** Копирует хеш транзакции в буфер обмена

**Поведение:**
- Использует `navigator.clipboard.writeText()`
- Показывает Toast уведомление "Хеш скопирован!"
- Hover эффект: зеленый акцент

**Иконка:** `Copy` (lucide-react)

**Toast компонент:**
- Автоматически исчезает через 2 секунды
- Анимация slide-in справа
- Glassmorphism стиль
- Можно закрыть вручную (X кнопка)

---

### 3. 🔍 Детали
**Функция:** Разворачивает дополнительную информацию о транзакции

**Показывает:**
- ✅ **Статус:** Успешно (зеленая точка)
- ⛽ **Gas Fee:** ~0.0001 NEAR
- 🔗 **Хеш:** Полный хеш транзакции (с переносом)

**Поведение:**
- Toggle кнопка (показать/скрыть)
- Анимация fade-in при раскрытии
- Hover эффект: фиолетовый акцент
- Активная кнопка подсвечена фиолетовым

**Иконка:** `Info` (lucide-react)

---

## 🎨 Дизайн

### Apple-Style Минимализм

**Кнопки:**
```css
- Glassmorphism фон (glass-subtle)
- Тонкие границы (border: 1px transparent)
- Hover: цветная граница + цветная иконка
- Размер: px-3 py-1.5 (компактные)
- Шрифт: text-xs (12px)
- Gap: gap-1.5 (иконка + текст)
```

### Адаптивность
- **Desktop:** Показывает иконку + текст
- **Mobile:** Только иконка (text hidden на sm)

### Hover Эффекты
| Кнопка | Hover Color | Border Color |
|--------|------------|--------------|
| Explorer | Blue-400 | Blue-400/30 |
| Копировать | Green-400 | Green-400/30 |
| Детали | Purple-400 | Purple-400/30 |

---

## 🌓 Dark/Light Mode Support

### Dark Mode
```css
- Background: glass-subtle (полупрозрачный)
- Text: text-secondary (светло-серый)
- Hover: яркие цвета (blue/green/purple-400)
- Border: полупрозрачная
```

### Light Mode
```css
- Background: glass-subtle (белый с прозрачностью)
- Text: text-secondary (темно-серый)
- Hover: насыщенные цвета
- Border: четкая
```

---

## 📱 Анимации

### Button Appearance
```css
.opacity-0 group-hover:opacity-100
transition-all duration-200
```

**Поведение:**
- Кнопки скрыты по умолчанию
- Появляются при hover на карточку
- Плавная анимация 200ms

### Details Expansion
```css
.animate-fade-in
```

**Эффект:**
```css
@keyframes fade-in {
  from: opacity 0, translateY(10px)
  to: opacity 1, translateY(0)
}
```

### Toast Notification
```css
.animate-slide-in
```

**Эффект:**
```css
@keyframes slide-in {
  from: translateX(100%), opacity 0
  to: translateX(0), opacity 1
}
```

---

## 🧩 Компоненты

### Toast.jsx
**Создан новый компонент**

**Props:**
- `message` (string) - Текст уведомления
- `onClose` (function) - Callback закрытия
- `duration` (number) - Время показа (default: 2000ms)

**Features:**
- ✅ Автоматическое закрытие
- ✅ Ручное закрытие (X кнопка)
- ✅ Glassmorphism стиль
- ✅ CheckCircle иконка
- ✅ Анимация slide-in

**Позиция:**
```css
fixed top-4 right-4 z-50
```

---

## 🔧 Технические Детали

### State Management

**TransactionsScreen.jsx:**
```javascript
const [expandedTx, setExpandedTx] = useState(null);
const [toast, setToast] = useState(null);
```

### Functions

**1. copyToClipboard:**
```javascript
const copyToClipboard = async (hash) => {
  try {
    await navigator.clipboard.writeText(hash);
    setToast('Хеш скопирован!');
  } catch (err) {
    console.error('Failed to copy:', err);
    setToast('Ошибка копирования');
  }
};
```

**2. toggleDetails:**
```javascript
const toggleDetails = (hash) => {
  setExpandedTx(expandedTx === hash ? null : hash);
};
```

---

## 📁 Измененные Файлы

### Новые:
- ✅ `webapp/src/components/Toast.jsx` - Toast компонент

### Обновленные:
- ✅ `webapp/src/components/TransactionsScreen.jsx` - Кнопки действий
- ✅ `webapp/src/index.css` - Новые анимации (slide-in, fade-in)

### Документация:
- ✅ `TRANSACTION_BUTTONS.md` - Этот файл
- ✅ `deploy-buttons.bat` - Deployment скрипт

---

## 🎯 Примеры Использования

### Desktop View
```
┌─────────────────────────────────────┐
│ 🔥 Claim HOT                        │
│ 2.50 NEAR ≈ $12.50                  │
│ ⏰ 15 минут назад                   │
│                                     │
│ [🌐 Explorer] [📋 Копировать] [🔍 Детали] │
└─────────────────────────────────────┘
```

### Mobile View
```
┌─────────────────────┐
│ 🔥 Claim HOT        │
│ 2.50 NEAR           │
│ ⏰ 15 минут назад   │
│                     │
│ [🌐] [📋] [🔍]     │ ← Только иконки
└─────────────────────┘
```

### Expanded Details
```
┌─────────────────────────────────────┐
│ 🔥 Claim HOT                        │
│ [Кнопки]                            │
│ ─────────────────────────────────── │
│ Статус:    ● Успешно                │
│ Gas Fee:   ~0.0001 NEAR             │
│ Хеш:       ABC123...XYZ789          │
└─────────────────────────────────────┘
```

---

## 🧪 Testing Checklist

### Функционал:
- [ ] Explorer открывается в новой вкладке
- [ ] Копирование работает
- [ ] Toast появляется после копирования
- [ ] Детали разворачиваются/сворачиваются
- [ ] Кнопки появляются при hover

### Анимации:
- [ ] Кнопки плавно появляются (200ms)
- [ ] Toast slide-in справа
- [ ] Детали fade-in при раскрытии

### Адаптивность:
- [ ] Desktop: иконка + текст
- [ ] Mobile: только иконка

### Темы:
- [ ] Dark Mode: полупрозрачные кнопки
- [ ] Light Mode: белые кнопки
- [ ] Hover цвета правильные

---

## 🐛 Troubleshooting

### Проблема: Копирование не работает
**Причины:**
- HTTPS not enabled (clipboard API requires secure context)
- Browser permissions

**Решение:**
- Убедитесь что сайт на HTTPS
- Проверьте browser console

---

### Проблема: Кнопки не появляются
**Причины:**
- CSS class `group` отсутствует на родителе
- Tailwind не скомпилирован

**Решение:**
```bash
cd webapp
npm run build
```

---

### Проблема: Toast не исчезает
**Причины:**
- useEffect cleanup не работает

**Решение:**
- Проверьте что `onClose` передан в Toast
- Проверьте browser console на errors

---

## 🔮 Будущие Улучшения

### Запланировано:
- [ ] "Повторить транзакцию" кнопка
- [ ] "Поделиться" кнопка (копирует ссылку)
- [ ] Реальные Gas Fee данные из API
- [ ] Статус из данных транзакции (не хардкод)
- [ ] Дополнительные детали (block height, receiver, etc)

### Идеи:
- Экспорт транзакций в CSV/JSON
- Фильтрация по типу транзакции
- Поиск по хешу
- QR код для транзакции

---

## 📊 Performance Impact

### Bundle Size:
- Toast.jsx: ~1 KB
- Новые анимации CSS: ~0.5 KB
- Иконки (3 новые): ~2 KB
- **Total:** ~3.5 KB

### Runtime:
- Clipboard API: Native (fast)
- State updates: Minimal re-renders
- Animations: GPU accelerated

---

## ✅ Result

### До:
```
❌ Placeholder кнопки (неактивные)
❌ Нет копирования
❌ Нет деталей транзакции
❌ Нет уведомлений
```

### После:
```
✅ 3 функциональные кнопки
✅ Копирование с Toast уведомлением
✅ Раскрывающиеся детали
✅ Apple-style минимализм
✅ Адаптивный дизайн
✅ Dark/Light mode support
```

---

## 🚀 Deployment

### Quick:
```bash
deploy-buttons.bat
```

### Manual:
```bash
cd webapp && npm install && cd ..
git add .
git commit -m "feat: Add transaction action buttons"
git push
```

---

**🎉 Transaction buttons готовы к использованию!**
